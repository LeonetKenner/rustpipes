[package]
name = "rustpipes"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
[target.'cfg(windows)'.dependencies]
named_pipe = "0.4.1"
[target.'cfg(unix)'.dependencies]
nix = {version="0.31.2", features=["fs"]}
